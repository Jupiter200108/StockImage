<% if item.content.present?%>
  <% if item.content.content_type.include?('image/')%>
    <%= image_tag item.content,class:"card-img-top image-fluid size" %>
  <% else %>
    <%= video_tag rails_blob_path(item.content), class: "card-img-top image-fluid size",controls: true %>
  <% end %>
<% end %>